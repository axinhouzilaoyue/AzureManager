{
  "name": "azure-manager",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun tests/regions.ts && bun tests/switch-race.ts && bun tests/verify-changes.ts && bun tests/switch-ui.ts",
    "test:regions": "bun tests/regions.ts",
    "test:api": "bun tests/switch-race.ts",
    "test:ui": "bun tests/switch-ui.ts",
    "test:verify": "bun tests/verify-changes.ts"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.6.2",
    "typescript": "^5.9.3"
  }
}
