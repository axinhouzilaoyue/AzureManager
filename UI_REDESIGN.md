# Azure Manager UI 重构设计记录

## 目标

- 账户详情不再默认占用主工作区。
- 账户列表在 Mac 常见浏览器宽度下能够完整展示。
- VM 表格不再因为双重固定侧栏产生横向滚动。
- 成本、AI 配额、订阅状态保持可见但不过度抢占空间。
- 继续使用当前浅色、卡片、表格、Modal、Toast 视觉体系。
- 保持个人项目所需的低复杂度和低资源占用。

## 方案讨论

| 方案 | 核心思路 | 主要问题 |
|---|---|---|
| A 单侧栏合并 | 全局导航和账户列表合并为一个 320～340px 侧栏 | 侧栏职责密，概览/设置页也会被账户区占用 |
| B 顶部账户切换器 | 顶部账户切换器 + 全宽 VM 工作台 + 420～520px 账户弹出层 | 账户列表不再常驻可见 |
| C 双栏响应式 | 保留全局导航和账户栏，自动折叠并重排 VM 表格 | 1280px 下横向预算过紧，依赖多个断点同时生效 |

三套方案经过独立设计、交叉评审和响应式宽度计算后，最终选择方案 B。

原因：

- 账户弹出层宽度可达到 420～520px，比原 280px 账户栏更容易显示长邮箱、订阅名和指标。
- VM 工作台获得完整宽度，1280、1440、1512px 下无需横向滚动。
- 账户详情仍通过按钮打开 Modal，符合按需查看的要求。
- 只需维护一个账户切换浮层，不需要同时协调两个固定侧栏。
- 个人使用场景下比 A、C 更稳定，后续扩展空间更好。

## 最终布局

```text
全局导航
└── 账户工作台
    ├── 顶部：账户切换器、账户列表、添加账户
    ├── 当前账户摘要：消费、AI 配额、到期状态
    ├── VM 搜索和状态筛选
    └── VM 表格

账户列表弹出层
├── 搜索账户
├── 名称排序、到期排序
├── 导入、导出
└── 账户列表

账户详情
└── 点击“账户详情”按钮后打开 Modal
```

### 账户切换器

- 页面顶部显示当前账户名称，长邮箱自动省略。
- 点击后打开 420～520px 的账户弹出层。
- 弹出层内部账户列表独立滚动，不发生横向滚动。
- 点击账户后关闭弹出层，并切换右侧 VM 工作台。
- 小屏幕下弹出层改为固定全屏区域。

### 账户详细信息

- 默认不在主页面展示完整凭据。
- 点击“账户详情”按钮后打开 Modal。
- Modal 内展示：
  - 账户名称、邮箱、订阅名称、订阅状态
  - Client ID、Tenant ID、Subscription ID
  - Client Secret，默认隐藏，可显示和复制
  - 到期时间
  - 本月、累计、历史消费
  - AI 配额层级
  - 查询时间和错误原因
- 关闭 Modal 后立即清除 DOM 中的明文 Secret。

### 成本与 AI 配额

- 主工作台展示紧凑摘要：
  - AI 配额
  - 本月消费
  - 累计消费
  - 历史消费
  - 数据更新时间
- 点击摘要打开账户详情 Modal。
- 查询失败时保留旧缓存，不覆盖上次成功金额。
- 错误按 HTTP 状态和 Azure `error.code` 分类，不再统一显示“赞助订阅请去官网查看”。

### VM 列表

- VM 表格是页面主体。
- 支持按名称、资源组、IP、区域和规格搜索。
- 支持按全部、运行中、已停止筛选。
- VM 表格最小宽度调整为 800px，不再让所有表格共享 860px 最小宽度。
- 保持现有创建、启动、停止、重启、更换 IP 和删除能力。

## 响应式策略

- 账户工作台不再受 1180px 最大宽度限制。
- 1280、1440、1512px 下 VM 表格完整显示，无横向滚动。
- 1024px 下全局导航自动收缩，VM 工作台保持可用。
- 980px 以下账户弹出层改为固定全屏区域。
- 700px 以下 VM 表格允许横向滚动，后续可替换为卡片列表。

## 实测结果

使用无头 Chrome 对以下宽度执行布局验证：

| 视口 | VM 表格横向滚动 | 页面横向滚动 |
|---:|---|---|
| 1024px | 无 | 无 |
| 1280px | 无 | 无 |
| 1440px | 无 | 无 |
| 1512px | 无 | 无 |
| 700px | 有，预期移动端模式 | 无 |

账户弹出层宽度为 520px，账户列表没有横向溢出。

## 实施状态

- 账户列表已从常驻 280px 侧栏迁移为顶部账户切换弹出层。
- 账户详情继续使用按需 Modal。
- 账户卡片已压缩为摘要行。
- 成本与配额已改为紧凑摘要条。
- VM 搜索和状态筛选已加入。
- 账户信息读取按需执行，不默认加载 Client Secret。
- 成本失败原因和缓存覆盖逻辑已修正。
