# Azure Manager UI 重构设计记录

## 目标

- 账户详细信息不再默认占用主工作区。
- 账户切换、VM 操作、任务查看保持在同一工作流中。
- 成本、AI 配额、订阅状态保持可见但不抢占 VM 列表空间。
- 继续使用当前浅色、卡片、表格、Modal、Toast 视觉体系。
- 保持个人项目所需的低复杂度和低资源占用。

## 评审方案

| 方案 | 核心思路 | 主要问题 |
|---|---|---|
| A 账户工作台优先 | 紧凑账户栏 + VM 工作台 + 账户详情弹窗 | 长期大量账户时列表管理能力一般 |
| B 资源优先控制台 | 顶部账户切换器 + 资源画布 + 详情抽屉 + 独立任务中心 | 入口和状态过多，对个人项目偏重 |
| C 总览优先下钻 | 总览健康卡片 + 紧凑账户表格 + 工作台 + 详情弹窗 | 日常 VM 操作多一步，总览聚合需谨慎控制请求 |

## 交叉评审结果

三位设计 Agent 使用相同的 8 个维度进行交叉评审：

- 首屏信息效率
- 账户详情可达性
- 成本与配额可理解性
- 个人使用低复杂度
- 当前项目迁移成本
- 响应式移动端
- 长期账户数量增长
- 视觉与当前 UI 延续性

评审结论：

- B 的整体复杂度和状态同步成本最高。
- A 和 C 的综合得分接近。
- A 更适合当前“选择账户后高频管理 VM”的使用方式。
- C 的总览、紧凑账户管理和移动端钻取思路值得吸收。
- 最终不完整照搬任何一套，采用融合方案。

## 最终方案

### 页面结构

```text
全局导航
├── 概览
├── 账户工作台
│   ├── 左侧紧凑账户列表
│   └── 右侧当前账户工作区
└── 设置

账户详情
└── 点击“账户详情”按钮后打开 Modal
```

### 账户工作台

- 左侧账户栏固定为约 280px，支持搜索、排序、导入、导出和拖动排序。
- 账户卡片压缩为三行以内：
  - 账户名称、订阅、到期状态
  - VM 数量
  - 本月消费、AI 配额
- 点击账户立即切换右侧工作台，不跳转页面。
- 右侧工作台顶部展示账户状态和主要操作：
  - 刷新
  - 创建虚拟机
  - 账户详情

### 账户详细信息

- 默认不在主页面展示完整凭据。
- 点击“账户详情”按钮后打开 Modal。
- Modal 内展示：
  - 账户名称、邮箱、订阅名称、订阅状态
  - Client ID、Tenant ID、Subscription ID
  - Client Secret，默认隐藏，可显示和复制
  - 到期时间
  - 本月、累计、历史消费
  - AI 配额层级
  - 查询时间和错误原因
- 关闭 Modal 后立即清除 DOM 中的明文 Secret。

### 成本与 AI 配额

- 主工作台只展示一行紧凑摘要：
  - AI 配额
  - 本月消费
  - 累计消费
  - 历史消费
  - 数据更新时间
- 点击任一摘要项打开账户详情 Modal。
- 完整数据状态、缓存状态、查询时间和失败原因在 Modal 中展示。
- 成本查询失败不影响 VM 列表和工作台操作。

### VM 列表

- VM 表格保持页面主体。
- 支持按名称、资源组、IP、区域和规格搜索。
- 支持按全部、运行中、已停止筛选。
- 保持现有创建、启动、停止、重启、更换 IP 和删除能力。

### 移动端

- 980px 以下账户栏和账户工作台改为单列。
- 账户详情 Modal 保持可滚动。
- VM 表格保留横向滚动，优先保证操作完整性。
- 后续如需进一步优化，可将 VM 表格切换为卡片列表。

## 实施状态

- 账户详情从主页面迁移到按钮触发的 Modal。
- 账户卡片已压缩。
- 成本与配额已改为紧凑摘要条。
- VM 搜索和状态筛选已加入。
- 账户信息读取按需执行，不默认加载 Client Secret。
- 成本失败原因已简化为用户可理解的状态。
