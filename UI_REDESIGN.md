# Azure Manager UI 重构设计记录

## 目标

- 账户详情不再默认占用主工作区。
- 账户列表在 Mac 常见浏览器宽度下能够完整展示。
- VM 表格不再因为双重固定侧栏产生横向滚动。
- 成本、AI 配额、订阅状态保持可见但不过度抢占空间。
- 继续使用当前浅色、卡片、表格、Modal、Toast 视觉体系。
- 保持个人项目所需的低复杂度和低资源占用。

## 方案讨论

| 方案 | 核心思路 | 主要问题 |
|---|---|---|
| A 单侧栏合并 | 全局导航和账户列表合并为一个 320～340px 侧栏 | 侧栏职责密，概览/设置页也会被账户区占用 |
| B 顶部账户切换器 | 顶部账户切换器 + 全宽 VM 工作台 + 420～520px 账户弹出层 | 账户列表被收进下拉框，必须点开才能看到 |
| C 双栏响应式 | 保留常驻账户栏，账户行只保留邮箱 / VM 台数 / 剩余天数 | 需要给 VM 表格预留足够宽度 |

方案 B 曾短暂采用，但账户列表被锁在下拉框里，违背“账户列表要直接能看到”的使用习惯，因此弃用。

最终采用 **C（常驻窄账户栏）+ 做减法**：

- 账户行只保留三项信息：**邮箱、VM 台数、剩余天数**，不再堆叠订阅名、消费、Tier。
- 信息量减少后账户栏只需 264px（≤1360px 时 228px），常驻可见且不会挤压 VM 表格。
- VM 工作台仍获得完整宽度，1024、1180、1280、1440、1512px 下均无横向滚动。
- 账户详情仍通过「详情」按钮或工作台右上角按钮打开 Modal，默认不占主工作区。

## 最终布局

```text
全局导航
└── 账户工作台（左右两栏）
    ├── 左栏：常驻账户列表（264px）
    │   ├── 账户计数 + 名称排序 / 到期排序
    │   ├── 搜索账户
    │   ├── 账户行：邮箱 · VM 台数 · 剩余天数 ·「详情」
    │   └── 导入 / 导出
    └── 右栏：当前账户工作台
        ├── 当前账户摘要：消费、AI 配额、到期状态
        ├── VM 搜索和状态筛选
        └── VM 表格

账户详情
└── 点击账户行「详情」或工作台右上角「账户详情」后打开 Modal
```

### 账户列表（常驻）

- 常驻左栏，不使用下拉框，切换页面后仍然可见。
- 每个账户行只显示三项信息：邮箱（超长省略号）、VM 台数、剩余天数。
- 剩余天数按紧急程度着色：≤7 天红色、≤30 天橙色、其余绿色；未设置到期显示灰色。
- 悬停或选中时出现拖动把手（`⠿`）和「详情」按钮，静止状态保持干净。
- 账户行点击即切换右侧 VM 工作台，不产生跳转。
- 账户列表内部独立滚动，不产生横向滚动。

### 账户详细信息

- 默认不在主页面展示完整凭据。
- 点击“账户详情”按钮后打开 Modal。
- Modal 内展示：
  - 账户名称、邮箱、订阅名称、订阅状态
  - Client ID、Tenant ID、Subscription ID
  - Client Secret，默认隐藏，可显示和复制
  - 到期时间
  - 本月、累计、历史消费
  - AI 配额层级
  - 查询时间和错误原因
- 关闭 Modal 后立即清除 DOM 中的明文 Secret。

### 成本与 AI 配额

- 主工作台使用单行摘要条代替原本的 5 张独立卡片：
  `AI 配额 · 本月 · 累计 · 历史 …… 更新`
- 各字段竖线分隔，更新时间靠右对齐，整条可点击。
- 点击摘要条打开账户详情 Modal。
- 工作台顶部原本有两个同名「刷新」按钮（刷新虚拟机 / 刷新账户信息），
  已合并为一个「刷新」，同时刷新订阅信息、消费与虚拟机列表。
- 查询失败时保留旧缓存，不覆盖上次成功金额。
- 错误按 HTTP 状态和 Azure `error.code` 分类，不再统一显示“赞助订阅请去官网查看”。

### VM 列表

- VM 表格是页面主体，从 7 列压缩为 6 列：虚拟机 / 规格 / 状态 / 开机 / 公网 IP / 操作。
- 区域并入「虚拟机」副行（`资源组 · 区域`），系统盘并入「规格」副行（`规格 / 系统盘 64 GB`）。
- 状态列用颜色圆点替代文字徽标：
  - 绿色 = 运行中，红色 = 已停止，琥珀色 = 启动中/停止中，灰色 = 未知
  - 具体文字通过 `title` 悬停查看，并保留 `aria-label`
- 操作列由 5 个按钮（启动/停止/重启/换 IP/删除）改为单个「操作」按钮 + 浮窗菜单：
  - 菜单 148px 宽，右对齐按钮，空间不足时自动向上翻转
  - 点击外部、按 Esc、滚动或缩放窗口自动关闭
  - 打开后 160ms 内忽略滚动关闭，避免 macOS 触控板惯性滚动导致菜单闪现
  - 删除项使用危险色并与其余操作分隔
- 支持按名称、资源组、IP、区域和规格搜索；支持按全部、运行中、已停止筛选。
- VM 表格最小宽度 720px（≤1360px 时 640px）。
- 保持现有创建、启动、停止、重启、更换 IP 和删除能力。

## 响应式策略

- 账户工作台不再受 1180px 最大宽度限制，并使用更小的水平内边距。
- 账户栏 264px；≤1360px 收窄为 228px，同时全局导航自动收缩为图标栏。
- 账户栏在 ≤980px 时改为上下堆叠，账户列表限高 260px 后内部滚动。
- 1024、1180、1280、1440、1512px 下 VM 表格完整显示，无横向滚动。

## 实测结果

使用无头 Chrome 注入模拟账户和 VM 数据后执行布局验证：

| 视口 | 账户栏 | 账户栏可见 | 账户列表横向溢出 | VM 表格横向滚动 | 页面横向滚动 |
|---:|---|---:|---:|---|---|
| 1024px | 228px | 是 | 无 | 无 | 无 |
| 1180px | 228px | 是 | 无 | 无 | 无 |
| 1280px | 264px | 是 | 无 | 无 | 无 |
| 1440px | 264px | 是 | 无 | 无 | 无 |
| 1512px | 264px | 是 | 无 | 无 | 无 |

交互验证：

- 账户行点击切换当前账户，账户栏保持可见。
- 点击「详情」打开 Modal，且不会误触发账户切换。
- 账户行文案为 `邮箱 · N 台 · 剩余 M 天`。

## 实施状态

- 账户列表为 264px 常驻左栏，不再使用顶部切换下拉框。
- 账户行已减到邮箱、VM 台数、剩余天数三项。
- 账户详情继续使用按需 Modal（账户行「详情」+ 工作台右上角「账户详情」）。
- 成本与配额已改为紧凑摘要条。
- VM 搜索和状态筛选已加入。
- 账户信息读取按需执行，不默认加载 Client Secret。
- 成本失败原因和缓存覆盖逻辑已修正。
