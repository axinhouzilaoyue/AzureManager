# Azure Manager 待增加能力与验收清单

> 本文档用于记录后续待补充能力、实现约束和验收标准。  
> 当前状态：本轮确认范围内的功能已实现，待结合真实 Azure 账号完成验收。  
> 已通过：TypeScript 类型检查、前端构建、数据库迁移测试、登录/导入/排序/导出/全局 SSH API 冒烟测试，以及无头 Chrome 登录、账户页、创建弹窗和设置页交互测试。  
> 待完成：使用真实 Azure 订阅验证成本、配额、Policy、镜像创建、NSG 和换 IP。  
> 参照项目：`/Users/liukaixin02/Downloads/CloudManager_SourceCode` 的 Azure 面板。  
> 仅参考功能行为，不照搬其代码结构、桌面 GUI 实现或明文凭据存储方式。

## 1. 项目定位

当前项目是一个基于 Bun + TypeScript + SQLite 的 Azure 多账号管理面板，主要面向个人使用，目标是：

- 管理多个 Azure Service Principal 账号。
- 查看和管理 Azure 虚拟机。
- 提供必要且直观的 Azure 创建和运维能力。
- 保持部署简单、资源占用低、运行流畅。
- 在个人使用场景下避免过度设计。

## 2. 实现约束

以下要求适用于本次所有新增能力，并作为后续验收的必要条件。

| 编号 | 要求 | 验收说明 |
|---|---|---|
| R-01 | 代码结构清晰、可扩展 | 新增能力按现有 `server`、`lib`、`public` 分层组织；Azure API、业务逻辑、数据库访问和 UI 展示不要混合在同一个文件中 |
| R-02 | 适合个人使用，避免过度复杂 | 不引入不必要的微服务、消息队列、独立前端工程、复杂权限系统或重型基础设施 |
| R-03 | 沿用当前 UI 风格 | 继续使用当前简洁、浅色、卡片、表格、弹窗、Toast 和按钮风格，不引入另一套视觉体系 |
| R-04 | 交互简洁、符合直觉 | 常用操作应能直接找到；危险操作需明确确认；加载、成功、失败和空状态应清楚可见 |
| R-05 | 资源占用低、运行流畅 | 优先复用 Bun、SQLite 和现有原生前端；避免常驻重复轮询、无上限内存缓存和不必要的大依赖 |
| R-06 | 不破坏现有安全能力 | Azure Client Secret 继续加密存储；登录、会话、账户验证和任务审计不能被削弱 |
| R-07 | 长操作继续后台执行 | VM 创建、启动、停止、重启、删除、换 IP 等耗时操作应继续使用后台任务并记录日志 |
| R-08 | 保持数据可迁移 | 新增配置和缓存应存储在 `data/` 或 SQLite 中，继续支持复制 `data/` 完成迁移 |
| R-09 | 控制复杂度 | 每项能力优先采用最小可用实现，确有必要时再抽象，不提前建设通用插件体系 |
| R-10 | 可验收 | 每项能力都应有明确入口、结果反馈、失败处理和验收标准 |

## 3. 状态说明

| 状态 | 含义 |
|---|---|
| 待开发 | 已确认需要补充，进入后续开发范围 |
| 待确认 | 需求尚未确定，暂不开发 |
| 不实现 | 已明确不进入当前项目范围 |

## 4. 待增加能力

### 4.1 成本、配额与订阅信息

| ID | 能力 | 具体要求 | 验收标准 | 优先级 | 状态 |
|---|---|---|---|---|---|
| COST-01 | Azure 本月消费 | 查询当前订阅 Month-to-Date 消费金额 | 账户详情可显示本月消费；金额和货币单位正确 | P0 | 已实现（待验收） |
| COST-02 | Azure 累计消费 | 查询可获取范围内的累计消费 | 账户详情可显示累计消费；无权限时给出明确状态而非空白 | P0 | 已实现（待验收） |
| COST-03 | 历史消费 | 根据到期时间和其他可用范围计算历史消费 | 能展示历史消费或明确说明无法计算 | P0 | 已实现（待验收） |
| COST-04 | 货币单位 | 解析 Cost Management 返回的币种 | 成本和货币单位同时展示 | P0 | 已实现（待验收） |
| COST-05 | 成本查询重试 | 对 HTTP 429 和 5xx 自动重试 | 默认最多重试 5 次；支持 Retry-After；重试过程不阻塞 UI | P0 | 已实现（待验收） |
| COST-06 | 成本缓存 | 保存最近一次有效成本结果 | 本次刷新失败时可以沿用上次有效结果，并明确提示为缓存数据 | P0 | 已实现（待验收） |
| QUOTA-01 | AI 配额层级 | 查询 Cognitive Services Tier 0 / Tier 1 | 账户详情能显示层级；不支持或查询失败时显示明确状态 | P0 | 已实现（待验收） |
| PROV-01 | Provider 自动注册 | 自动注册 Microsoft.Compute、Microsoft.Network、Microsoft.CostManagement | 首次连接或按需注册；注册失败不阻断其他可用功能，并记录日志 | P0 | 已实现（待验收） |

### 4.2 账号管理

| ID | 能力 | 具体要求 | 验收标准 | 优先级 | 状态 |
|---|---|---|---|---|---|
| ACC-01 | JSON 批量导入账号 | 支持从 JSON 文件导入 Azure 账号 | 导入成功后账号列表刷新；无效文件和不完整凭据给出明确错误 | P1 | 已实现（待验收） |
| ACC-02 | JSON 导出账号 | 支持导出账号配置 | 默认不直接输出明文 Secret；若允许包含 Secret，必须有明确警告和显式选择 | P1 | 已实现（待验收） |
| ACC-03 | 账号名称模糊搜索 | 按别名、邮箱或显示名称过滤 | 输入关键字后列表即时过滤；清空后恢复完整列表 | P1 | 已实现（待验收） |
| ACC-04 | 账号名称排序 | 支持名称升序和降序 | 排序结果稳定；刷新页面后行为符合预期 | P1 | 已实现（待验收） |
| ACC-05 | 账号到期时间排序 | 按订阅到期时间排序 | 无到期日期的账号有明确排序位置；排序结果可恢复 | P1 | 已实现（待验收） |
| ACC-06 | 账号拖动排序 | 用户可调整账号显示顺序 | 顺序持久化到数据库；刷新、重新登录和迁移后仍保留 | P1 | 已实现（待验收） |

### 4.3 区域、策略与规格

| ID | 能力 | 具体要求 | 验收标准 | 优先级 | 状态 |
|---|---|---|---|---|---|
| REG-01 | Policy 区域过滤 | 读取 Azure Policy 的允许区域并过滤区域列表 | 创建 VM 时不展示被 Policy 明确禁止的区域；策略读取失败时采用合理降级 | P0 | 已实现（待验收） |
| NET-01 | IP 权限检测 | 判断区域支持 Basic、Standard 或两者 | 创建 VM 时只提供当前区域允许的 IP 类型；无法判断时允许手动选择 | P1 | 已实现（待验收） |
| SIZE-01 | 免费规格精确筛选 | 检查 B1s、B2ats_v2、B2pts_v2 是否在当前区域可用 | 只把实际可用的免费规格显示为推荐项；不可用时允许手动输入其他规格 | P1 | 已实现（待验收） |
| VM-INFO-01 | 系统盘大小展示 | VM 列表展示 OS Disk 大小 | 列表中能看到系统盘容量；暂无数据时显示明确占位符 | P1 | 已实现（待验收） |
| VM-INFO-02 | 更换 IP 保留属性 | 更换公网 IP 时保留旧 IP 的 SKU 和 Static/Dynamic 模式 | 新 IP 的 SKU 和分配模式与原 IP 一致；无旧 IP 时使用安全默认值 | P0 | 已实现（待验收） |

### 4.4 创建虚拟机能力

| ID | 能力 | 具体要求 | 验收标准 | 优先级 | 状态 |
|---|---|---|---|---|---|
| IMG-01 | Ubuntu 24.04 | 增加 Ubuntu 24.04 镜像 | 可在支持的区域内选择并成功创建 Ubuntu 24.04 | P1 | 已实现（待验收） |
| IMG-02 | CentOS 8 | 增加 CentOS 8 镜像 | 可在支持的区域内选择并成功创建 CentOS 8 | P1 | 已实现（待验收） |
| IMG-03 | ARM64 镜像 | 根据 VM Size 自动选择 ARM64 或 x86_64 SKU | ARM64 规格使用 ARM64 镜像；x86_64 规格使用对应镜像；不支持时提示清楚 | P1 | 已实现（待验收） |
| DISK-01 | 扩展磁盘大小 | 支持 30、32、64、128、256、512、1024、2048 GiB | 前端和后端校验一致；不合法大小被拒绝并有提示 | P1 | 已实现（待验收） |
| DISK-02 | 多磁盘类型 | 支持 Premium SSD、Standard SSD、Standard HDD | 三种类型可正确传入 Azure；默认类型保持合理且明确 | P1 | 已实现（待验收） |
| VM-CREATE-01 | 自定义 VM 名称 | 创建时允许填写 VM 名称 | 留空时自动生成；非法名称在提交前提示；避免与现有资源命名冲突 | P1 | 已实现（待验收） |
| VM-CREATE-02 | 自定义用户名 | 创建时允许填写管理员用户名 | 支持合法用户名；非法或 Azure 不接受的用户名有明确提示 | P1 | 已实现（待验收） |
| VM-CREATE-03 | 自定义密码 | 创建时允许填写密码，也可自动生成 | 密码满足 Azure 复杂度要求；创建结果中可以复制登录信息 | P1 | 已实现（待验收） |
| SSH-01 | 全局 SSH 公钥 | 保存全局 SSH 公钥并在创建 VM 时注入 | 可在设置中维护公钥；创建时可选择使用；私钥不保存到项目数据库 | P1 | 已实现（待验收） |
| SSH-02 | 全局 SSH 用户名密码 | 保存全局 SSH 用户名和密码，创建时复用 | 凭据加密保存；创建时可选择使用；UI 不回显完整密码 | P1 | 已实现（待验收） |
| ROOT-01 | 自动启用 root | 创建后按用户选择配置 root SSH 登录 | 这是显式选项，不默认开启；开启时显示安全风险提示；关闭时不影响普通用户登录 | P1 | 已实现（待验收） |
| NSG-01 | 自动创建 NSG | 创建 VM 时自动创建并绑定 NSG | NSG、规则和网卡绑定关系正确；创建失败时随资源组一起清理 | P1 | 已实现（待验收） |
| NSG-02 | 自动开放端口 | 支持配置常用端口或自定义端口规则 | 默认不静默开放全部入站/出站；开放 22 端口可作为常用选项；全部端口开放必须二次确认 | P1 | 已实现（待验收） |

## 5. 待确认能力

| ID | 能力 | 当前状态 | 需要确认的问题 | 建议 |
|---|---|---|---|---|
| SSH-TERM-01 | SSH 内置终端 | 待确认 | 是否需要浏览器内 SSH 终端；是否支持密码、私钥、Host Key 校验和会话超时 | 若个人使用频率高，可放到第二阶段；若只是偶尔登录，先提供 SSH 配置和复制命令 |

## 6. 明确不实现

以下能力已确认不进入当前项目范围。

| 能力 | 处理结果 | 原因 |
|---|---|---|
| 自动 DD 重装 | 不实现 | 不符合当前项目简洁、安全和低复杂度目标 |
| Cloudflare 创建时绑定 | 不实现 | 暂不引入外部 DNS 自动化 |
| Cloudflare 换 IP 同步 | 不实现 | 暂不引入外部 DNS 自动化 |
| VM 本地重命名 | 不实现 | 暂不需要本地显示名映射 |

## 7. 建议实施阶段

| 阶段 | 范围 | 目标 |
|---|---|---|
| 第一阶段 | Cost、Quota、Provider、Policy、IP 权限、免费规格、系统盘展示、换 IP 保留属性 | 先补齐订阅信息和 VM 信息正确性 |
| 第二阶段 | 新镜像、ARM64、磁盘类型、自定义名称/用户/密码、全局 SSH、root、NSG | 补齐创建 VM 的核心能力 |
| 第三阶段 | 账号导入导出、搜索、名称排序、到期时间排序、拖动排序 | 提升多账号日常使用效率 |
| 暂不实施 | DD、Cloudflare、VM 本地重命名 | 保持项目边界清晰 |
| 待确认 | SSH 内置终端 | 单独确认产品形态和安全方案 |

## 8. 通用验收要求

后续所有新增能力至少满足：

- [ ] 不破坏现有登录、账号切换和会话逻辑。
- [ ] 不破坏现有 VM 创建、生命周期操作、换 IP 和任务系统。
- [ ] 不降低 Azure Client Secret 的存储安全性。
- [ ] 长耗时操作有后台任务、状态反馈和错误日志。
- [ ] 页面操作有加载中、成功、失败和空状态。
- [ ] Azure 权限不足、接口限流和服务异常都有可理解的提示。
- [ ] 新增字段有数据库迁移方案，已有 `data/` 数据可平滑升级。
- [ ] 新增依赖经过必要性评估，不引入明显增加启动时间或内存占用的组件。
- [ ] TypeScript 类型检查通过。
- [ ] 主要流程在 Docker 环境和本地环境中行为一致。
- [ ] UI 风格与当前项目保持一致，桌面和常见移动宽度下均可正常使用。
- [ ] 本次范围内所有标记为“已实现（待验收）”的能力均有明确验收结果。

## 9. 当前决策汇总

- 需要补充能力：27 项。
- 明确不实现：4 项。
- 待确认：1 项。
- 当前阶段：功能已实现，等待真实 Azure 环境验收。
